const initialState = {
  form: {
    email: '',
    password: '',
  },
  isLogged: false,
};

const user = (state = initialState, action = {}) => {
  switch (action.type) {
    default:
      return state;
  }
};

export default user;
